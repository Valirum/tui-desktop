# 🖥️ TUI Desktop — Рабочий стол в терминале

**TUI Desktop** — это интерактивный текстовый пользовательский интерфейс (TUI), реализующий аналог рабочего стола прямо в терминале Linux. Проект разработан как курсовая работа и демонстрирует возможности управления файлами, запуска команд и навигации без графической оболочки.

> 💡 **TUI = Text-based User Interface**  
> Всё управление — через клавиатуру и мышь (если поддерживается терминалом).

---

## 🌟 Основные возможности

- 🖱️ **Поддержка мыши**: клики по иконкам, перетаскивание (Shift + стрелки), навигация.
- 📁 **Файловый менеджер в виде иконок**: папки, исполняемые `.sh`-файлы, текстовые документы, изображения.
- ⭐ **Избранное**: закрепляйте часто используемые файлы и папки.
- 🖼️ **Фон из изображений**: автоматическая конвертация изображений в ASCII-арт.
- 🔌 **Мульти-вкладочность**: запуск любых терминальных программ во вкладках (например, `nano`, `htop`, `bash`).
- 🕒 **Интеграция с системой**: отображение времени, заряда батареи (для ноутбуков на Linux).
- 📅 **Встроенный календарь** и **меню быстрого запуска**.
- ⌨️ **Горячие клавиши** (в стиле `tmux`): префикс `Ctrl+B`, затем команда.

---

## 🚀 Установка и запуск

### Требования

- Linux (проверено на Arch Linux / Manjaro)
- Python 3.8+
- Библиотеки: `curses`, `pyte`, `PIL` (Pillow), `psutil`

### Установка зависимостей

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
